# Арифметические операции и магические методы в Python

Короткий обзор урока о том, как делать объекты своих классов «арифметическими» — то есть позволяющими использовать операторы `+`, `-`, `*`, `/`, `//`, `%` и их варианты.

## 🔹 Основные методы

Используются, когда объект стоит **слева** в выражении:

- `__add__` — сложение (`+`)
- `__sub__` — вычитание (`-`)
- `__mul__` — умножение (`*`)
- `__truediv__` — деление (`/`)
- `__floordiv__` — целочисленное деление (`//`)
- `__mod__` — остаток (`%`)

## 🔹 Отражённые операции

Срабатывают, когда объект находится **справа** или левый операнд не поддерживает операцию:

- `__radd__`, `__rsub__`, `__rmul__`, `__rtruediv__`, `__rfloordiv__`, `__rmod__`

Пример:  
`5 + obj` → попытка вызвать `obj.__radd__(5)`.

## 🔹 Операции с присваиванием

Используются в выражениях вида `+=`, `-=`, `*=`, и позволяют изменять объект «на месте»:

- `__iadd__`, `__isub__`, `__imul__`, `__itruediv__`, `__ifloordiv__`, `__imod__`

## 🔹 Пример класса

```python
class PiggyBank:
    def __init__(self, coins):
        self.coins = coins

    def __repr__(self):
        return f"PiggyBank({self.coins})"

    def __add__(self, other):
        if isinstance(other, int):
            return PiggyBank(self.coins + other)
        if isinstance(other, PiggyBank):
            return PiggyBank(self.coins + other.coins)
        return NotImplemented

    def __radd__(self, other):
        return self.__add__(other)
