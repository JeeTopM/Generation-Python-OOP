# Унарные операторы и функции в Python

В этом уроке рассматриваются **унарные операторы** и связанные с ними **магические методы**, с помощью которых можно переопределять их поведение для собственных классов.

---

## 🔹 Унарные операторы и их магические методы

В Python унарные операторы (`+`, `-`, `~`) работают через специальные (магические) методы:

| Оператор | Метод | Назначение |
|-----------|--------|-------------|
| `+x` | `__pos__()` | Определяет поведение **унарного плюса** |
| `-x` | `__neg__()` | Определяет поведение **унарного минуса** |
| `~x` | `__invert__()` | Определяет поведение **оператора инвертирования** |

> ⚙️ Из встроенных типов оператор `~` определён только для `int` и `bool`.  
> Результат выражения `~x` вычисляется как `-(x + 1)`.

---

## 🔹 Магические методы для встроенных функций

Некоторые встроенные функции (такие как `abs()`, `round()` и функции из модуля `math`) также используют специальные методы, если они определены в классе:

| Функция | Метод | Описание |
|----------|--------|-----------|
| `abs(x)` | `__abs__()` | Определяет, что возвращает функция `abs()` |
| `round(x, n)` | `__round__(n)` | Определяет поведение `round()`; аргумент `n` — количество знаков после запятой |
| `math.trunc(x)` | `__trunc__()` | Определяет усечение числа до целого |
| `math.floor(x)` | `__floor__()` | Определяет округление вниз |
| `math.ceil(x)` | `__ceil__()` | Определяет округление вверх |

---

## 🧭 Итог

Магические методы позволяют настраивать реакцию пользовательских объектов на:
- **унарные операторы** (`+`, `-`, `~`)
- **встроенные функции**, работающие с числами (`abs`, `round`, `math.trunc`, `math.floor`, `math.ceil`)

Это делает классы более гибкими и “естественными” при работе с ними, как с обычными числами.
